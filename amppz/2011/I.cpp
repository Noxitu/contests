#include <cstdio>
#include <climits>
#include <map>

struct Vertex : std::map<Vertex*, int> {
	
};

Vertex INF[444], MAT[444];

int main() {
	int n, m, k;
	scanf("%d %d %d", &n, &m, &k );
	while( k --> 0 ) {
		int a, b;
		scanf("%d %d", &a, &b );
		
	}
}
