#include <iostream>
#include <algorithm>
#include <set>
 
using namespace std;

void test() {
    int n, w;
    cin >> n >> w;
    int T[n];
    for( int i = 0; i < n; i++ )
        cin >> T[i];
        
    

}

 
int main() {
    ios_base::sync_with_stdio(false);
    int T;
    cin >> T;
    while( T --> 0 )
        test();

}